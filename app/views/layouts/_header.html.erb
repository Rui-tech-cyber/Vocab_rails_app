<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #6f42c1;">
  <div class="container-fluid">
    <a class="navbar-brand fw-bold text-white" href="<%= root_path %>">Vocab</a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" 
      aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto align-items-center">
        <% if user_signed_in? %>

          <li class="nav-item me-3">
            <span class="nav-link text-white">ようこそ、<%= current_user.name %>さん</span>
          </li>

          <li class="nav-item me-2">
            <%= link_to "マイページ", my_page_path, class: "nav-link text-white fw-semibold" %>
          </li>

          <li class="nav-item">
            <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-outline-light ms-2", form: { class: "d-inline" } %>
          </li>
        <% else %>

          <li class="nav-item me-3">
            <span class="nav-link text-white">ようこそ、ゲストさん</span>
          </li>

          <li class="nav-item">
            <%= link_to "ログイン", new_user_session_path, class: "btn btn-light fw-semibold ms-2" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
