<h1><%= @user.name %>さんのマイページ</h1>

<h2>最後にテストした単語帳</h2>

<% if @exams.any? %>
  <table class="table">
    <thead>
      <tr>
        <th>単語帳</th>
        <th>最後に学習した日</th>
        <th>正解数</th>
        <th>不正解数</th>
        <th>結果を見る</th>
      </tr>
    </thead>
    <tbody>
      <% @exams.each do |exam| %>
        <tr>
          <td><%= exam.word_book.title %></td>
          <td>
            <% exam_date = exam.created_at.in_time_zone("Asia/Tokyo").to_date %>
            <% today = Time.current.in_time_zone("Asia/Tokyo").to_date %>
            <% days_ago = (today - exam_date).to_i %>
            <%= case days_ago
                when 0
                  "今日"
                when 1
                  "昨日"
                else
                  "#{days_ago}日前"
                end %>
          </td>
          <td><%= exam.exam_answers.select(&:correct).count %></td>
          <td><%= exam.exam_answers.size - exam.exam_answers.select(&:correct).count %></td>
          <td><%= link_to "結果を見る", result_exam_path(exam), class: "btn btn-sm btn-primary" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p>まだ学習履歴はありません。</p>
<% end %>

<%= link_to "作成した単語帳一覧を確認する", word_books_path, class: "btn btn-secondary mt-3" %><br />
<%= link_to "トップページに戻る", root_path, class: "btn btn-secondary mt-3" %>
